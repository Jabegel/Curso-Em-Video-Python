import random
# Biblioteca que randomiza números

num = random.randint(1,20)
# Para gerar um número aleatorio basta apenas colocar dessa forma 'random.random()'
# Para gerar um número aleatorio entre x e y basta apenas colocar dessa forma 'random.randint(x,y)'

print(num)